<!-- p2p-agents-managed: michael-codex-override-v1 -->
# Michael Snoyman's Codex defaults

This is the managed user-level Codex bootstrap for Michael Snoyman. Its canonical source is `https://github.com/snoyberg/p2p-agents/blob/master/bootstrap/michael-codex-AGENTS.override.md`.

Read repository-local agent instructions as well. Project-specific technical and workflow requirements may intentionally refine these defaults, but do not infer authorization for external publication from an implementation request.

## Local implementation and review

When Michael asks for implementation, normally create coherent local candidate commits suitable for human review rather than leaving completed work uncommitted.

Michael's normal Codex review workflow is local commit-range review with `tuicr` unless he explicitly asks for another workflow or repository-local instructions intentionally specify a different process. Follow the current `workflows/tuicr.md` guidance when available.

After each candidate or review-fix commit, report the branch name, commit SHA, and exact commit range Michael can review.

Committing locally does not authorize publication. Do not push, open or close a pull request, request remote review, merge, deploy, or perform another externally visible repository action unless Michael clearly authorizes it.

When a pull request is authorized, keep its description concise by default: purpose, material reviewer context, and relevant verification rather than an implementation diary.

## Current guidance

When network access and task scope permit, consult the current Michael-specific guidance at `https://github.com/snoyberg/p2p-agents/blob/master/people/michael.md` and the current `tuicr` workflow at `https://github.com/snoyberg/p2p-agents/blob/master/workflows/tuicr.md` rather than assuming this bootstrap contains every preference.

This file intentionally contains the critical local-review and publication boundary itself so those defaults still apply when the canonical repository cannot be reached.
